# 知伴模块完成矩阵（2026-07-27）

基准：`zhiban_agent_architecture_20260705.html`。状态只按当前唯一工程
`/Users/laozhou/Desktop/Claude/zhiban-t31` 的生产入口、持久层和可重复测试判断。
页面存在、空回调、演示数据和旧文档结论都不计为完成。

| 模块 | 当前生产链路 | 本轮验证 | 状态与下一缺口 |
|---|---|---|---|
| App Shell / Navigation | 日历、关系、问问、能力、我的五个入口；问问进入独立对话页；设置子页使用 Navigation 返回栈 | 模拟器冷启动；日历→我的→语言→返回→安全→记忆逐项点击 | 可用。继续补全所有系统返回和进程恢复设备用例 |
| 问问 / 多模态输入 | 文本、图片、文件、录音转写、实时语音均进入统一 Runtime；Chat / Work 共用会话与输入组件 | `:app:testDebugUnitTest`、设备端回归 | 代码链路可用。真实 Provider 图片理解、真机麦克风/扬声器仍需带凭据和真机复验 |
| Agent Runtime | 感知、上下文、规划、工具循环、流式事件、取消/恢复、反馈投影 | 模块边界检查；runtime/context/feature-ask 模块测试 | 可用。跨域多工具并行 DAG 的用户可见进度仍需加强 |
| Provider | 阶跃星辰单 Provider；Key 进入 Keystore；连接检测后发布；流式文字/图片/ASR/Realtime 适配 | 编译、单元和设备端离线测试；本轮未发送真实外网模型请求 | 代码闭环。真实 Key、额度、外网与供应商服务状态属于 `NOT VERIFIED` |
| 记忆 | 会话上下文、长期记忆候选、确认、检索、删除、休眠和维护；用户可查看和手动添加 | 安全页→记忆真实路由；Memory/Room 设备测试 | 可用。跨设备同步未实现 |
| 工具 / MCP | Local Tool 与远端 Streamable HTTP MCP 统一经 CapabilityRouter、ActionPolicy、审计和确认 | mcp/tools/governance 模块测试；设备端 Router 测试 | 可用。Android 不支持 Stdio；MCP Resources/Prompts 尚未实现 |
| 日历 | 日历查看、新建、编辑、删除；Agent search/conflict/create/update/delete；写入前确认和 Undo；经用户授权、选择和确认后从系统日历导入，按系统 event/instance 幂等更新；通过 Android 系统编辑页强确认写回手机日历；用户和 Agent 工具创建的日程共用可更新、可取消的本地通知提醒；跨来源时间重叠先提示、由用户确认后可并存 | 模拟器日历首屏、编辑器、通知权限和系统 Job；完整连接测试；仓库测试覆盖提醒合同、确认绑定、调度回调、提醒持久化、19→20 迁移和重复导入更新；系统 Intent 交互验收 | 本机日历、只读导入、强确认写回、问问提醒、用户创建日程提醒和冲突确认可用。当前提醒采用 WorkManager，属于省电友好的近似时间提醒 |
| 联系人与关系图谱 | 联系人增删改查、画像事实、已确认关系、多重关系、关系证据、关系事件/共同经历、2-hop 图查询；系统通讯录选择导入；常用称呼与多平台身份；基于手机号、邮箱、微信、平台账号、别名等发现重复；用户选择主资料后非破坏合并并可完整恢复；通过 Android 系统编辑页强确认写回通讯录；联系人详情可打开系统拨号盘或短信编辑页，由用户最终执行；用户显式开启系统通知访问后，聊天通知只进入本机候选箱；短信、聊天等应用的文字可由系统分享面板主动送入同一待确认箱；候选必须由用户选择联系人后才生成可追溯证据，删除证据会回到候选箱 | 数据库 v16→v21 迁移；模拟器 Contacts Provider 导入；系统拨号和短信 Intent 的真实号码传递；通知访问未授权态、真实系统通知、真实 ACTION_SEND 分享、待确认箱、联系人确认和删除撤销 UI；仓库测试覆盖分享文本清洗/限长/去重、候选持久化/忽略/确认/撤销、身份增删、重复发现基础、非破坏合并/恢复；系统 Intent 交互验收 | 本机图谱、身份、去重、可撤销合并、强确认写回、安全拨号/短信以及通知/手动分享到联系人证据闭环可用；未申请受限的短信和通话记录权限 |
| 能力 / Skill | 内置场景 Skill；Work 意图激活；能力页展示依赖工具与 MCP 入口 | skills 模块测试；设备端回归 | 内置能力可用。第三方 Skill 安装、签名和可视化编排未实现 |
| 我的 / 设置 | 个人资料、智能体设置、语言、安全、存储、数据、报告错误、关于、外观 | 本轮新增七条真实路由；模拟器逐页验证；Impeccable 规则扫描 0 项 | 可用。当前没有账号系统，因此不展示虚假的“退出登录” |
| 数据 / 安全 | Room/SQLCipher、Keystore、私有附件、脱敏诊断、写操作确认、迁移链 | 196 项设备测试完成，1 项受控外网证书握手跳过，0 失败 | 本机安全边界可用。云同步、账号删除和数据导出产品链路未实现 |
| 可观测性 / 反馈 | 运行记录、指标、脱敏诊断、人类反馈进入后续上下文 | 单元和设备端回归 | 可用。正式错误上报服务未接入 |

## 本轮验收结果

- `./gradlew :app:testDebugUnitTest :app:lintDebug :app:assembleDebug`：通过。
- `verifyAgentModuleBoundaries` 与 Agent 独立模块测试：通过。
- Android 15 模拟器设备测试：196 项完成，1 项受控外网证书握手按条件跳过，0 失败。
- 真实系统通知 → 本机候选箱 → 用户选择联系人 → 生成证据 → 删除证据后重新进入候选箱：模拟器闭环通过。
- 修复 `FactIndexMigrationTest` 未注册 `MIGRATION_16_17` 导致的 v14→v17
  测试链断裂；单测和完整设备回归均重新通过。
- UI 规范扫描：本轮涉及的设置、我的和导航文件为 0 项问题。

## 继续实施顺序

1. 在不成为默认短信/电话/助手应用的前提下，不申请受限的 `READ_SMS`、`READ_CALL_LOG`；继续扩展用户主动分享和通知候选的来源识别。
2. 账号、云同步、数据导出/删除；完成后再出现“退出登录”。
3. 第三方 Skill 包签名、安装、权限清单和版本管理。
4. 真机文字、图片、ASR、全双工语音与弱网中断的真实 Provider 复验。
